# Dashboard runtime — what Streamlit Community Cloud installs.
# For the full local stack (training, API, tests) use requirements-full.txt.
streamlit
plotly
pandas
requests
wordcloud
matplotlib
fpdf2
supabase
dnspython
Authlib
# Built-in analysis engine (used when the FastAPI service isn't available,
# e.g. on the hosted demo)
torch
transformers
